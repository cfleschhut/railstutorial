<section>
  <h4>New micropost</h4>

  <%= form_for @micropost, html: { class: 'micropost-form' } do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <div>
      <%= f.text_area :content, rows: 4, maxlength: 140, placeholder: 'Compose new micropost…' %>
    </div>

    <%= f.submit 'Post this!', class: 'btn btn-block' %>
  <% end %>
</section>
